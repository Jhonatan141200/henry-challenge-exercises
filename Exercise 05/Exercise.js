/**
 * -------------------- AÑADIR UN MÉTODO A UN OBJETO ------------------------
 * Se te presenta la función añadirFuncion(cb) que recibe como argumento un callback 'cb',
 * que realiza el filtro de una lista pasa como argumento.
 * tu tarea será agregar una función al prototipo de "Array" con el nombre "filtroPersonalizado"
 * que implemente la lógica dada por cb.
 *     Ejemplo:
 *         listaEdades = [13, 23, 45, 12, 18,13,15]
 *         cb = function (lista) {
 *             return lista.filter(n => n >= 18)
 *         }
 *         añadirFuncion(cb)
 *         console.log(listaEdades.filtroPersonalizado()): Resultado [23, 45, 18]
 */


function addFunction(cb) {
    /* Code Here */

}






module.exports = {
    añadirFuncion
};